@model List<PortfolioBlock>

<h1>Anoarai</h1>

<div>
    @{
        @foreach(PortfolioBlock block in Model)
        {
            @(await Html.RenderComponentAsync<Website.Views.Shared.Component.PortfolioBlockTemplate>(RenderMode.ServerPrerendered, new { data = block }))
        }
    }
    
</div>